name: Release

# On a version tag push: run the full test suite, publish a self-contained
# win-x64 build, package it with Inno Setup, and attach the installer to
# a GitHub Release — docs/AGENTS.md's Packaging/Release Agent: "a Windows
# installer as a GitHub Release asset, produced automatically on tag
# push." Trigger with e.g. `git tag v0.1.0 && git push origin v0.1.0`.

on:
  push:
    tags:
      - 'v*'

jobs:
  build-installer:
    runs-on: windows-latest

    # softprops/action-gh-release needs to create the Release and upload the
    # installer asset — the default GITHUB_TOKEN is read-only otherwise (403
    # "Resource not accessible by integration").
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Determine version from tag
        id: version
        shell: pwsh
        run: |
          $tag = "${{ github.ref_name }}"
          $version = $tag -replace '^v', ''
          "version=$version" >> $env:GITHUB_OUTPUT

      - name: Restore
        run: dotnet restore ChronoTravelers.sln

      # -m:1 serializes the solution's test projects — see ci.yml's Test
      # step for why (ChronoTravelers.Server.Tests's real-socket
      # TelnetConnectionTests flaking under cross-project CPU contention).
      # Same fix here so a release isn't blocked by that flake either.
      - name: Test
        run: dotnet test ChronoTravelers.sln -c Release -m:1

      - name: Publish self-contained win-x64 build
        run: dotnet publish src/ChronoTravelers.Console/ChronoTravelers.Console.csproj -c Release -r win-x64 -o publish/win-x64 -p:Version=${{ steps.version.outputs.version }}

      - name: Ensure Inno Setup is available
        shell: pwsh
        run: choco install innosetup --no-progress -y

      - name: Build installer
        shell: cmd
        run: '"C:\Program Files (x86)\Inno Setup 6\ISCC.exe" /DMyAppVersion=${{ steps.version.outputs.version }} installer\ChronoTravelers.iss'

      - name: Upload installer to GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: installer/Output/*.exe
          generate_release_notes: true
